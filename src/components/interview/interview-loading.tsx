import LoadingSpinner from '../loading-spinner';

export default function InterviewLoading() {
  return (
    <div className="flex items-center justify-center h-[88vh] w-screen">
      <LoadingSpinner />
    </div>
  );
}
